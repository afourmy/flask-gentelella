version: '3'
services:
  gentelella:
    build: .
    ports:
      - "5000:5000"
    restart: always
    depends_on:
      - db
  db:
    image: postgres:10
    env_file: .env
    expose:
      - 5432